#include "parser.hpp"

namespace ExpressionParser
{
    size_t Parser::parse_expression()
    {
        // while (!this->current_token()->is_eol())
        // {

        // }
        return 10;
    }
}
